## Frontend environment variables
## Copy to `.env` for local overrides

# Backend API base URL (optional).
# If unset, the app should default to http://localhost:8000 in code.
VITE_API_URL=http://localhost:8000

